# Chatbot server base URL (use this in production; in dev we proxy /api)
VITE_CHAT_API_URL=

# Vite dev proxy target for chatbot API
VITE_PROXY_TARGET=http://localhost:3001

# Mock Bank API (if not using dev proxy)
VITE_BANK_API_URL=

# Dev proxy target for Bank API
VITE_BANK_API_TARGET=http://localhost:4000
